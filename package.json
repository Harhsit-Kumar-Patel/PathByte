{
  "name": "pathbyte",
  "version": "1.0.0",
  "description": "PathByte - Tech Stack Recommendation Engine",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
