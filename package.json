{
  "name": "pathbyte",
  "version": "1.0.0",
  "description": "PathByte - Tech Stack Recommendation Engine",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm ci"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
